#include "config.hpp"
#include <string>

namespace htm_swat {

Config loadConfig(const std::string& config_path) {
    // TODO: Parse YAML file
    // For now just return defaults
    Config config;
    return config;
}

} // namespace htm_swat

